{
  "name": "meu-projeto",
  "version": "1.0.0",
  "description": "Este é um projeto simples que demonstra algumas operações básicas, como soma, subtração, multiplicação e divisão.",
  "main": "src/main.js",
  "scripts": {
    "start": "node src",
    "test": "jest",
    "lint": "eslint src",
    "format": "prettier --write src"
  },
  "dependencies": {
    "jest": "^26.1.0"
  },
  "devDependencies": {
    "jest": "^26.1.0"
  }
}
